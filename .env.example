# ============================================
# API Keys - Free Tier Optimized Configuration
# ============================================

# Financial Modeling Prep (FMP) - RECOMMENDED
# Get free key: https://financialmodelingprep.com/developer/docs/
# Free tier: 250 requests/day
# Used for: Company overview, financial statements (income, balance, cashflow)
FMP_API_KEY=your_fmp_key_here

# Finnhub - RECOMMENDED
# Get free key: https://finnhub.io/register
# Free tier: 60 requests/minute
# Used for: Price data (primary)
FINNHUB_API_KEY=your_finnhub_key_here

# ============================================
# Legacy API Keys (Optional - Fallback Only)
# ============================================

# Alpha Vantage - OPTIONAL
# Get free key: https://www.alphavantage.co/support/#api-key
# Free tier: 25 requests/day (very limited)
# Used as fallback when FMP/Finnhub unavailable
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key_here

# ============================================
# Provider Selection (Optional)
# ============================================

# Price data provider (default: finnhub)
# Options: finnhub (recommended), stooq (no key, EOD only), alpha_vantage (legacy)
PRICE_PROVIDER=finnhub

# Company overview provider (default: fmp)
# Options: fmp (recommended), alpha_vantage (legacy)
OVERVIEW_PROVIDER=fmp

# Financial statements provider (default: fmp)
# Options: fmp (recommended), alpha_vantage (legacy)
FINANCIAL_PROVIDER=fmp

# News provider (default: gdelt)
# Options: gdelt (no key required, recommended), finnhub (requires key)
NEWS_PROVIDER=gdelt

# ============================================
# Environment
# ============================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
